$("#comments").modal('hide');
$("#comments").replaceWith("<%= escape_javascript(render partial: 'index', locals: {comments: @comments, comment: @tweet.comments.build}) %>");
$("#comments").modal('show');
$("#comments").scrollTop($("form").offset().top);
